import { BookingFrequency } from './booking-frequency.model';

describe('BookingFrequency', () => {
  it('should create an instance', () => {
    expect(new BookingFrequency()).toBeTruthy();
  });
});
